# Serving image dependencies -- exact pins, torch-free by design.
# The package is installed with --no-deps on top of these, so torch and
# torchvision (base dependencies, needed only for training) never enter the
# container. openCV stays: the calibrated threshold is only valid if serving
# decodes and resizes exactly as calibration did. See preprocess.py.
fastapi==0.135.3
uvicorn==0.44.0
onnxruntime==1.28.0
mangum==0.21.0
pydantic==2.12.5
numpy==2.1.3
opencv-python-headless==4.11.0.86
pillow==11.1.0
python-multipart==0.0.20
